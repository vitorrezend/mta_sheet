# Configuração do banco de dados SQLite
# Você pode alterar o caminho do arquivo sem recompilar o projeto.
# Se não for definido, o padrão será 'mta_sheet.db' no diretório raiz.
DATABASE_URL=sqlite:mta_sheet.db
