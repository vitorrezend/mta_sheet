# MTA Character Manager Environment Configuration

# Database path (relative to project root or absolute)
# Default: mta_sheet.db
# Example: DATABASE_URL=sqlite:my_game_session.sqlite
DATABASE_URL=sqlite:mta_sheet.db
